{
  "name": "my-period-app",
  "version": "1.0.0",
  "private": true,
  "description": "My Period - mobile period tracker built with Capacitor",
  "scripts": {
    "build": "vite build",
    "cap:sync": "npm run build && npx cap sync android",
    "android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "vite": "^7.1.5"
  }
}
